read A, b
(n, m) = size(A)
a = augmentedMatrix (A, b)
if n = m then
	for k = 1 to n - 1
		major = 0
		rowm = k
		for p = k to n
			if major < |apk| then
				major = |apk|
				rowm = p
			end if
		end for
		if major = 0 then
			break, infinite solutions
		else
			if fila m != k then
				for j = 1 to n + 1
					aux = a(k, j)
					a(k, j) = a(rowm, j)
					a(rowm, j) = aux
				end for
			end if
		end if
		for i = k + 1 to n
			mik = aik / akk
			for j = k to n + 1
				aij = aij - mikakj
			enf for
		end for
	end for
	for i = n to 1
		sum = 0
		for p = i + 1 to n
			sum = sum + aipxp
		end for
		xi = (bi - sum) / aii
	end for
else
	matrix isn't square
end if
print a
print x