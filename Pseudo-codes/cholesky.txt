Input MatrixA
Input MatrixB

n = Size of MatrixA
L = MatrixB* n since position 0 until the size of MatrixB

for i < size of matrixA
	for k < since range i+1
		sum= sum(L[i][j] * L[k][j] for j < inside of range
	if i is equial to K
	L[i][k] = root(Matrix[i][i]-sum

	return velue L

print result