function jacobi(A, b, x , normVal, tol, iters,w)
    D = diagonal(A)
    if(D has 0) return err
    L = -1 * tril(A) + D
    U = -1 * triu(A) + D
    T = inv(D-w*L)*(1-w)*(D+w*U)
    C = w*(inv(D-(w*L))*b)
    SR= max(abs(eigvals(T)))
    if(SR > 1) return err
    ite = 0
    while(norm > tol && ite < iteMax)
        xold = x
        x = (T*xold)+C
        iters.add(ite, norm, x)
        norm = norm(xold-x,normVal)
        ite += 1
    return iters